[package]
name = "formalizer"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["formalizer"]
license = "Apache-2.0"
description = "Renders pixel-perfect PDF form replicas from a PyMuPDF-extracted schema."
